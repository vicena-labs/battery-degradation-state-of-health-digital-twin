# Contributing

Part of the [Vicena Research Twins collection](https://vicena.ai).

Install with `pip install -e .[dev]`, run `pytest -q`, then reproduce the baseline scripts. New models need units, assumptions, citations, tests, baseline comparisons, and validation boundaries. Never mix records from the same physical cell across calibration and validation. Do not commit confidential or unlicensed data.
